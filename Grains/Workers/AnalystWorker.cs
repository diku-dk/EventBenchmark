using System;
namespace Grains.Workers
{
	public class AnalystWorker
	{
		// not many, 10-50 probably

		// they will query the results of the continuous queries
		// they will update theprices
		// they will remove products from the catalog. rare.
	}
}

